// We us the Type Function to create an instance of said type 
let favNum = String(5) // string
let favNum2 = Number('5.0') // number
let favNum3 = new Number('5.0') // a look ahead. This is a "type construction"

// When we just write the js notion of the type ie... 
let litString = 'This is a string'
let litNum = 5.5
let litArr = [1, 2]
let litObj = {
    name: 'lauren',
    age: 26
}

